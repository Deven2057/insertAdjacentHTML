first.insertAdjacentHTML('beforebegin', '<div class="text">  beforebegin </div> ' )
first.insertAdjacentHTML('beforeend', '<div class="text">  before end </div> ' )
first.insertAdjacentHTML('afterbegin', '<div class="text">  after begin </div> ' )
first.insertAdjacentHTML('afterend', '<div class="text">  after end </div> ' )
